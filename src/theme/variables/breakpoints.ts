export interface Breakpoints {
  lg: number;
  md: number;
  sm: number;
  xs: number;
}

const breakpoints: Breakpoints = {
  lg: 1150,
  md: 1000,
  sm: 767,
  xs: 520,
};

export default breakpoints;
